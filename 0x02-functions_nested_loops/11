#include "main.h"

/**
 * add - add two numbers
 *
 * @a : first num
 * @b : second num
 *
 * Return: always return  0
 */
int add(int a, int b)
{
	int add = a + b;

	/* while(add)
	{
		int tmp = add % 10;
		_putchar(tmp + '0');
		add /= 10;
	} */
	return (add);
}

